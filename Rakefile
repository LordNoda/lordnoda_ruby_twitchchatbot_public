# This defines the default call for ($ rake) to run as ($ rake <%w[run]>)
task default: %w[run]

# This defeines the run task which is called with ($ rake run)
# This can be read as do the following cmd commands and can contain more than 1 call
# This is generally defined to run the main entry point for the application
task :run do
  ruby 'bot.rb'
end
